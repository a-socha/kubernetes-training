spring:
  application:
    name: kubernetes-training-consumer
  profiles:
    active: ${PROFILE:local}
  cloud:
    kubernetes:
      config:
        name: ${spring.application.name}
        namespace: default
